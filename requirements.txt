# Generated from AST import scan of src/ and ui/ (excluding empty stubs and notebooks).
# Versions pinned from the environment verified on Python 3.11 (2026-05-31).
# scikit-learn 1.7.0 matches models/imputer.pkl (SimpleImputer); recreate venv after pull.
#
# Direct imports: fastapi, pydantic, numpy, pandas, joblib, lightgbm, streamlit
# Runtime (artifact load): scikit-learn, scipy — required to unpickle models/model.pkl
# Server: uvicorn — documented runner in src/api/main.py (not imported in code)

fastapi==0.136.3
uvicorn==0.48.0
pydantic==2.11.7

numpy==2.2.4
pandas==2.3.0
joblib==1.5.1
lightgbm==4.6.0
scikit-learn==1.7.0
scipy==1.15.2

streamlit==1.46.1
plotly==6.0.1
altair==5.5.0
shap==0.48.0
groq==0.30.0
